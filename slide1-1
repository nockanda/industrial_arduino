//산업용 아두이노에 전원이 들어오면 최우선적으로 딱 1회만 실행되는 부분!(초기화부분)
void setup() {
  //산업용 아두이노와 PC가 통신을 하기 위해서는 아래 코드가 필요하다!
  Serial.begin(9600);
}

//setup이후에 반복해서 실행되는 부분!(해야할일)
void loop() {
  //1초에 한번마다 특정한 메시지를 전송한다!
  Serial.println("나는 산업용 아두이노입니다!"); //산업용 아두이노에서 PC로 전송하기!
  delay(1000); //1초간 쉰다!
}
